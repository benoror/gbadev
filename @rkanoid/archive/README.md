# Archive

This folder stores non-source preservation artifacts.

## Subfolders

- `build-artifacts/` - generated outputs from historical/experimental builds (`*.bin`, `*.o`, etc.)
- `emulator-saves/` - emulator runtime saves and state files preserved by version

Keep this folder append-only where possible.

