# Legacy snapshot: `20_ago_0`

Historical source snapshot preserved as-is.

## Provenance

- Folder name suggests capture date/version checkpoint (`20_ago_0`).
- Includes gameplay source, startup/data asm, and build scripts.

## Known differences vs `current/`

- Earlier/alternate iteration of bonus and tile conventions.
- Includes `copia_de.c` as additional in-progress variant material.
- Asset completeness is partial in current repository state.

## Expected build entry point

- `make2.bat` and/or `make_mb.bat` depending on target output.

Both expect legacy ARM250-era toolchain executables.

