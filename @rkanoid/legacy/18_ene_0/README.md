# Legacy snapshot: `18_ene_0`

Historical source snapshot preserved as-is.

## Provenance

- Folder name suggests capture date/version checkpoint (`18_ene_0`).
- Contains full source/build scripts for that checkpoint plus partial assets.

## Known differences vs `current/`

- Slight gameplay logic differences from later snapshots.
- Uses its own `data.asm` and tool scripts.
- Asset completeness is partial in current repository state.

## Expected build entry point

- `make.bat` (ROM)
- `make_mb.bat` (MultiBoot)

Both expect legacy ARM250-era toolchain executables.

