# Legacy bitmap art

Preserved BMPs from the original codebase that are **not** consumed by the asset
pipeline. They remain here for historical reference and potential future reuse.

The active source art for the ROM lives in `../source_art/`. See
`tools/rebuild_assets.py` for the mapping that generates `bitmaps/*.raw` and
`palettes/*.pal`.
